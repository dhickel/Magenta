server:
  port: 8080

app:
  ai:
    config-path: ./config/ai-config.example.json

magenta:
  alpha-access:
    username: ${MAGENTA_ALPHA_USERNAME:alpha}
    password: ${MAGENTA_ALPHA_PASSWORD:change-me-alpha}
  features:
    schedules-enabled: true
    reactions-enabled: true
  plan:
    execution-stream-timeout-seconds: 0
  ai:
    openai-compatible-read-timeout-seconds: 360
  # Docker runtime removed in favor of filesystem-backed workspaces.
  # docker.enabled defaults to false; the Docker services are conditionally
  # disabled and will be deleted in a follow-on phase.

spring:
  web:
    resources:
      chain:
        enabled: true
  datasource:
    url: jdbc:sqlite:./chat-memory.db?foreign_keys=true
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  autoconfigure:
    exclude:
      - org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiEmbeddingAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiImageAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioSpeechAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioTranscriptionAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiModerationAutoConfiguration
  ai:
    ollama:
      base-url: http://192.168.1.112:11434
      chat:
        options:
          model: qwen3.6:35b

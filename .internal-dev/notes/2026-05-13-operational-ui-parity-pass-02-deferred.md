# Deferred After Pass 02

- Schedules and event reactions still have API support but no dedicated operational UI surface.
- Agent outputs still rely on job/run relationship traversal; direct artifact-level agent identity would simplify query and filtering.
- Workspace tab currently depends on workspace service availability; if unavailable it degrades with a placeholder message.

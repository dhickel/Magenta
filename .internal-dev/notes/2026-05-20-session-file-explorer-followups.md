# Session File Explorer Follow-ups

- If session lists become slow with many conversations or large chat file trees, add a cache or persisted chat file metadata index for `outputCount` instead of walking the filesystem on every session-list request.

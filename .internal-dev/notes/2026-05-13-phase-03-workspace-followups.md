# 2026-05-13 Phase 03 Workspace Follow-ups

- Consider adding a dedicated `/dashboard/workspaces` list panel that consumes `GET /api/workspaces` for operator-level browsing across agents/jobs.
- Consider exposing output directory hints from `WorkspaceDirectoryService` directly (instead of owner-type convention mapping) if nonstandard root layouts are introduced.

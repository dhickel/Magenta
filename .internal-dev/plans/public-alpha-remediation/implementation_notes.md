# Public Alpha Remediation Implementation Notes

## Purpose

This file is a shared, append-only coordination surface for agents executing the public-alpha remediation sprint plans. It is not source of truth over code or tests. Use it to record cross-domain discoveries, changed assumptions, validation gotchas, and decisions that later domain agents should see.

## Rules

- Append dated notes; do not rewrite another agent's notes except to correct an obvious typo in the same turn.
- Link to code, tests, `.internal-dev` artifacts, and commits when available.
- If a note changes scope or defers work, record the user-approved decision in the relevant domain plan and `progress.md` too.
- Do not use this file to skip a planned finding. Skips require an explicit user decision and an entry in `no-action-registry.md`.

## Notes

- 2026-05-18: Initial suite generated. Implementation has not started. All findings are `planned`.
- 2026-05-18: Subplan 01 of `01-security-access-control` added a method-based alpha gate in `AlphaSecurityConfiguration`: `GET`/`HEAD`/`OPTIONS` remain public, while all unsafe web/API routes require the configured `magenta.alpha-access` basic credential and CSRF. Current unsafe-route inventory includes the reviewed API mutations (`/api/settings/runtime`, `/api/agents`, jobs, projects, workspaces, workflows, plans, tasks, chat) plus browser/HTMX controls in `OrchestrationController` such as plan/workflow/job/project editors, inbox actions, agent lifecycle, queue controls, shell exec, history purge, and settings.
- 2026-05-18: Browser CSRF compatibility is centralized in `/js/alpha-security.js`, loaded by both public shells. It reads the `XSRF-TOKEN` cookie and adds `X-XSRF-TOKEN` to same-origin unsafe HTMX and fetch requests, with a small HTMX 401/403 error presenter. Future browser mutation routes should use the shared shells or deliberately provide the same CSRF behavior.
- 2026-05-18: Subplan 01 validation passed on commit `bf10a85`: focused tests passed and live app probes confirmed public read routes still return 200, unauthenticated unsafe mutation returns 401, authenticated HTMX mutation without CSRF returns a 403 fragment, and authenticated+CSRF mutation reaches controller/business validation.
- 2026-05-18: Operator instruction for future domain orchestrators: commit after each completed subplan before starting the next subplan. Already-running or already-run agents are not retroactively changed.
- 2026-05-18: Operator instruction for branch flow: create each next domain branch from the latest validated prior-domain/integration tip, because domains intentionally build on each other and may touch the same code targets.
- 2026-05-18: Subplan 02 of `01-security-access-control` centralized filesystem path segment id validation in `PlainPathSegmentValidator` and applied it at `AgentProfileService`, `WorkspaceService`, and `WorkspaceDirectoryService`. Agent ids now generate only when omitted as `null`; explicit blank, dot-only, separator, absolute-syntax, and percent-encoded path syntax ids fail validation. Implementer checks passed for the new validator/service/controller tests plus adjacent orchestration/workspace/output/shell tests; external validation is still pending.
- 2026-05-18: Subplan 02 validation passed on commit `b1418f4`: validator and adjacent `WorkspaceControllerTest` checks passed, and the validation agent confirmed agent profile, workspace, directory, and controller invalid-id rejection coverage for bug-02.
- 2026-05-18: Subplan 03 of `01-security-access-control` removed persisted workflow node value interpolation from the graph card and selected-node side panel in `src/main/resources/static/js/orchestration/workflows.js`. Node label/key/type/plan/message/config/port values now render through DOM text nodes or form `.value` assignment; the remaining `innerHTML` in that file is the static composer mount template. Implementer checks passed with `mvn -Dtest=WorkflowGraphComposerSecurityTest,OrchestrationControllerTest#workflowJsProvidesGraphComposerSurface test` and `node --check src/main/resources/static/js/orchestration/workflows.js`; focused browser/Playwright proof is still pending the validation subagent.
- 2026-05-18: Subplan 03 validation passed on commit `0c114bb`: focused tests and `node --check` passed, and live `/workflows` validation on an isolated SQLite DB confirmed script-like node label/key/message/config/port payloads remained inert after UI entry and persisted reload, with no alert, console execution, or injected `img`/`script` nodes.
- 2026-05-18: Subplan 04 of `01-security-access-control` added scoped assignment lifecycle overloads in `AssignmentService` and switched REST/HTMX lifecycle routes to pass the route `agentId` before cancel, pause, resume, or force interrupt mutation. Cross-agent route attempts now return 404-style lifecycle errors and do not mutate the target assignment; same-agent behavior and queue refresh rendering are preserved. Implementer checks passed with `mvn -Dtest=OrchestrationRuntimeTest,AgentOrchestrationControllerTest,OrchestrationControllerTest test`; bounded startup smoke reached successful Spring Boot startup before timeout shutdown. External validation is still pending.
- 2026-05-18: Subplan 04 validation passed on commit `f4b1978`: validation confirmed REST and HTMX lifecycle routes call scoped assignment service overloads, cross-agent attempts return non-2xx without mutation, and public controllers no longer call assignment-id-only lifecycle methods.
- 2026-05-18: `01-security-access-control` domain validation passed: focused changed-area tests, full `mvn test` with 467 tests, bounded Spring startup, live auth/CSRF/HTMX probes, and live `/workflows` XSS browser proof all passed. Evidence logs: `/tmp/magenta-security-domain-live-3317476.log`, `/tmp/magenta-security-domain-live-extra-3318140.log`, `/tmp/magenta-security-domain-playwright-3317476.log`.
- 2026-05-18: `02-workspace-tools-outputs` branch was created from the validated security integration tip. For subplan 01, interpret the plan's "yolo override" as an explicit opt-in operator override, not a default bypass of shell confinement.
- 2026-05-18: Subplan 01 of `02-workspace-tools-outputs` changed shell confinement so `*` in allowed shell commands is ignored unless `unsafeAllowWildcardShellCommands=true`, legacy profile seeding drops wildcard shell defaults when the override is off, and shell wrapper executables plus absolute/traversal argument patterns are rejected even under the unsafe wildcard override. Active orchestration contexts with `hostWorkspacePath` now resolve shell working directories against the run workspace, run output path, or current linked project scope instead of falling back to the broader agent workspace.
- 2026-05-18: Subplan 01 validation passed on commit `cddbb6c`: focused shell/config/runtime tests, config JSON parse, `git diff --check`, and bounded startup passed; validation confirmed wildcard shell commands are ineffective by default, the unsafe override remains constrained, active host workspace/output/project scope cwd resolution works, and `config/ai-config.example.json` no longer contains wildcard defaults or a real-looking API key.

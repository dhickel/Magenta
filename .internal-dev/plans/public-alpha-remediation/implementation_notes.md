# Public Alpha Remediation Implementation Notes

## Purpose

This file is a shared, append-only coordination surface for agents executing the public-alpha remediation sprint plans. It is not source of truth over code or tests. Use it to record cross-domain discoveries, changed assumptions, validation gotchas, and decisions that later domain agents should see.

## Rules

- Append dated notes; do not rewrite another agent's notes except to correct an obvious typo in the same turn.
- Link to code, tests, `.internal-dev` artifacts, and commits when available.
- If a note changes scope or defers work, record the user-approved decision in the relevant domain plan and `progress.md` too.
- Do not use this file to skip a planned finding. Skips require an explicit user decision and an entry in `no-action-registry.md`.

## Notes

- 2026-05-18: Initial suite generated. Implementation has not started. All findings are `planned`.
- 2026-05-18: Subplan 01 of `07-validation-harness-regression` added `PublicApiRouteBindingTest`, a Spring Boot `MockMvc` route-binding suite backed by an isolated SQLite database with `foreign_keys=true`. The suite covers public chat direct-execution rejection, plan/task submit SSE `event:submitted` names, workflow invalid-run SSE `event:failed`, and representative jobs/projects/agents/outputs/runtime/settings routes through the real Spring MVC context and alpha security filters. Mockito web-slice mocks were avoided because the Java 25 inline mock maker could not mock large service classes such as `ChatService` and `PlanService`; the full test context catches route binding and serialization drift without invoking model-backed chat turns.
- 2026-05-18: Subplan 01 validation passed before commit: parent and validator reran `mvn -Dtest=PublicApiRouteBindingTest,AlphaSecurityConfigurationTest,PublicRunSubmissionControllerTest,GlobalExceptionHandlerTest test` with 27 passing tests, `git diff --check`, and bounded isolated SQLite startup reaching `Started Magenta2Application`. Validator confirmed the new coverage addresses bug-17 without direct controller construction or model-backed chat streaming.
- 2026-05-18: Subplan 02 of `07-validation-harness-regression` added a checked-in live-app Playwright harness under `tests/playwright/` with `playwright.config.js`. The harness does not start Spring Boot; it defaults to `http://localhost:18080` with alpha credentials `alpha` / `test-alpha-password`, supports environment overrides, captures console/page/network/static-asset diagnostics, fails unexpected `500` and static asset failures, and separates the expected anonymous unsafe mutation auth failure from unexpected non-2xx responses. Focused coverage includes public page reachability, dynamic agent detail, mobile agent shell width/grid checks, HTMX plan draft save plus API/UI persistence, and HTMX workflow draft/node/validation/submit-panel persistence checks. Implementer validation ran `npx playwright --version` and `npx playwright test --list`; live execution remains pending because repository policy requires a validation subagent for Playwright runs.
- 2026-05-18: Subplan 02 first validation failed live Playwright execution with 2 passed / 3 failed tests: `New Plan` matched both `New Plan` and `New Plan Chat`, `New Workflow` matched two buttons, and the anonymous auth-gate probe omitted CSRF so Spring returned 403 before the Basic-auth gate. Serial fix scoped the plan/workflow draft buttons to the exact HTMX actions and now obtains an `XSRF-TOKEN` cookie with authenticated GET before making an unauthenticated POST carrying that token so the expected 401 isolates missing Basic auth.
- 2026-05-18: Subplan 02 second validation still failed live execution with 2 passed / 3 failed tests: the plan id was read with `innerText()` from closed details content, the workflow draft locator still matched both sidebar and empty-state buttons, and the auth-gate API context returned 200 because valid configured credentials were still applied. Serial fix reads draft ids from persisted editor form `hx-put` attributes, scopes workflow creation to `.browser-sidebar-header`, and uses invalid Basic credentials while carrying a valid CSRF cookie/header so the expected 401 isolates authentication failure.
- 2026-05-18: Subplan 02 third validation improved to 3 passed / 2 failed tests. Remaining issues were harness-only: the plan persistence test read the API before the HTMX save `PUT` completed, and workflow node `Add` matched both `Add` and `Add Route`. Serial fix now waits for `PUT /plans/_editor/{planId}` around the Save click and uses an exact `Add` button locator.
- 2026-05-18: Subplan 02 fourth validation improved to 4 passed / 1 failed test. The remaining workflow test clicked `Add` before the HTMX workflow save `PUT` had completed, so the node-add fragment could be overwritten by the save response. Serial fix now waits for `PUT /workflows/_editor/{workflowId}` around the Save click before adding a node.
- 2026-05-18: Subplan 02 validation passed after the serial fix pass: `npx playwright --version` returned `Version 1.59.1`, `npx playwright test --list` listed 5 Chromium tests, `git diff --check` passed, live Spring app on `:18080` with isolated SQLite `/tmp/domain07-subplan02-revalidation4.sqlite` reached `Started Magenta2Application`, and `MAGENTA_PLAYWRIGHT_BASE_URL=http://localhost:18080 MAGENTA_ALPHA_USERNAME=alpha MAGENTA_ALPHA_PASSWORD=test-alpha-password npx playwright test tests/playwright/public-alpha-harness.spec.js` passed 5/5 in 7.0s. Log scan found no unexpected `ERROR`, `Exception`, `500`, `404`, stale asset, HTMX, or SSE regressions.
- 2026-05-18: Subplan 03 of `07-validation-harness-regression` enabled `foreign_keys=true` across active repository/service/controller SQLite test fixtures and added `ScheduleReactionFeatureParitySpringTest`, a Spring context test that overrides the global test-disabled schedule/reaction flags to `true`, rejects invalid assignment templates, manually polls a due schedule, and publishes a matching reaction event. Enabling FK enforcement exposed that `run_output_artifacts.run_id` was incorrectly constrained to `plan_runs(id)` even though workflow final outputs persist workflow-run artifacts; `schema.sql` and `WorkspaceRepository` now keep artifact `run_id` polymorphic and warm-start migration recreates the old constrained table without that FK. Local implementer validation passed with the 283-test focused fixture set, a static SQLite URL scan with no missing `foreign_keys=true` hits, `git diff --check`, and bounded Spring startup.
- 2026-05-18: Subplan 03 validation passed: validator confirmed SQLite fixture scans have no relevant test JDBC URLs missing `foreign_keys=true`, the production-like schedule/reaction Spring test covers enabled flags with deterministic manual polling/publishing, the polymorphic `run_output_artifacts` FK fix is justified and covered by schema migration assertions, the focused Maven slice passed 283 tests, `git diff --check` passed, and bounded isolated SQLite startup reached `Started Magenta2Application`.
- 2026-05-18: Subplan 04 of `07-validation-harness-regression` added targeted regression-gap tests for ro-17 without changing production code. `PublicApiRouteBindingTest` now proves the removed plan direct-run route no longer binds, empty workflow REST/SSE submissions do not enqueue `WORKFLOW_RUN` assignments, and route-level agent lifecycle controls reject cross-agent cancellation without mutating the owner queue row. `OrchestrationControllerTest` adds a Jsoup-parsed workflow editor/node-panel XSS regression so persisted node payloads remain escaped DOM values rather than executable elements or event attributes. `WorkspaceRepositorySchemaMigrationTest` now asserts legacy `workspace_roots` migration preserves workspace owner/root/display/metadata fields as well as active, release-requested, and released leases. Implementer validation passed with the focused Maven slice for changed tests plus adjacent direct-run, workflow XSS, agent controller, and runtime suites; `git diff --check`; and bounded startup reaching `Started Magenta2Application` on port `34401` before timeout shutdown.
- 2026-05-18: Subplan 04 parent validation first failed because `PublicApiRouteBindingTest.agentAssignmentLifecycleRoutesRejectCrossAgentOwnership` assumed the assignment would remain `QUEUED`; in the real Spring context, the runner can legitimately advance it to `RUNNING` before the read-back assertion. Serial fix keeps the route-level ownership proof but now asserts the cross-agent cancel leaves the row owned by the original agent, does not set `CANCELLED`/`CANCEL_REQUESTED`, and does not appear in the other agent's assignment list.
- 2026-05-18: Subplan 04 validation passed after the serial test fix: focused Maven slice for route binding, orchestration DOM/XSS, workspace schema migration, public run submission, graph composer security, agent orchestration controller, and runtime tests passed; `git diff --check` passed; bounded isolated SQLite startup reached `Started Magenta2Application`. Validator confirmed coverage for direct-run route absence, empty workflow no-enqueue behavior, parsed-DOM workflow XSS, workspace-root warm migration field preservation, and route-level agent queue ownership.

- 2026-05-18: Domain `07-validation-harness-regression` validation passed on commit `3ce7ae7`: validator confirmed `git diff --check`, focused domain Maven tests with 176 passing tests, full `mvn test` with 549 passing tests, no SQLite test fixture URLs missing `foreign_keys=true`, Playwright 1.59.1 with five listed public-alpha harness tests, live Spring plus focused Playwright with five passing tests, clean startup, and warm startup. Evidence files: `/tmp/domain07-focused-maven.log`, `/tmp/domain07-full-mvn.log`, `/tmp/domain07-live-spring.log`, `/tmp/domain07-playwright-public-alpha.log`, `/tmp/domain07-clean-startup.log`, `/tmp/domain07-warm-startup.log`. The only dirty worktree file during validation was unrelated `.internal-dev/notes/idea_drop.md`.
- 2026-05-18: Subplan 01 of `01-security-access-control` added a method-based alpha gate in `AlphaSecurityConfiguration`: `GET`/`HEAD`/`OPTIONS` remain public, while all unsafe web/API routes require the configured `magenta.alpha-access` basic credential and CSRF. Current unsafe-route inventory includes the reviewed API mutations (`/api/settings/runtime`, `/api/agents`, jobs, projects, workspaces, workflows, plans, tasks, chat) plus browser/HTMX controls in `OrchestrationController` such as plan/workflow/job/project editors, inbox actions, agent lifecycle, queue controls, shell exec, history purge, and settings.
- 2026-05-18: Browser CSRF compatibility is centralized in `/js/alpha-security.js`, loaded by both public shells. It reads the `XSRF-TOKEN` cookie and adds `X-XSRF-TOKEN` to same-origin unsafe HTMX and fetch requests, with a small HTMX 401/403 error presenter. Future browser mutation routes should use the shared shells or deliberately provide the same CSRF behavior.
- 2026-05-18: Subplan 01 validation passed on commit `bf10a85`: focused tests passed and live app probes confirmed public read routes still return 200, unauthenticated unsafe mutation returns 401, authenticated HTMX mutation without CSRF returns a 403 fragment, and authenticated+CSRF mutation reaches controller/business validation.
- 2026-05-18: Operator instruction for future domain orchestrators: commit after each completed subplan before starting the next subplan. Already-running or already-run agents are not retroactively changed.
- 2026-05-18: Operator instruction for branch flow: create each next domain branch from the latest validated prior-domain/integration tip, because domains intentionally build on each other and may touch the same code targets.
- 2026-05-18: Subplan 02 of `01-security-access-control` centralized filesystem path segment id validation in `PlainPathSegmentValidator` and applied it at `AgentProfileService`, `WorkspaceService`, and `WorkspaceDirectoryService`. Agent ids now generate only when omitted as `null`; explicit blank, dot-only, separator, absolute-syntax, and percent-encoded path syntax ids fail validation. Implementer checks passed for the new validator/service/controller tests plus adjacent orchestration/workspace/output/shell tests; external validation is still pending.
- 2026-05-18: Subplan 02 validation passed on commit `b1418f4`: validator and adjacent `WorkspaceControllerTest` checks passed, and the validation agent confirmed agent profile, workspace, directory, and controller invalid-id rejection coverage for bug-02.
- 2026-05-18: Subplan 03 of `01-security-access-control` removed persisted workflow node value interpolation from the graph card and selected-node side panel in `src/main/resources/static/js/orchestration/workflows.js`. Node label/key/type/plan/message/config/port values now render through DOM text nodes or form `.value` assignment; the remaining `innerHTML` in that file is the static composer mount template. Implementer checks passed with `mvn -Dtest=WorkflowGraphComposerSecurityTest,OrchestrationControllerTest#workflowJsProvidesGraphComposerSurface test` and `node --check src/main/resources/static/js/orchestration/workflows.js`; focused browser/Playwright proof is still pending the validation subagent.
- 2026-05-18: Subplan 03 validation passed on commit `0c114bb`: focused tests and `node --check` passed, and live `/workflows` validation on an isolated SQLite DB confirmed script-like node label/key/message/config/port payloads remained inert after UI entry and persisted reload, with no alert, console execution, or injected `img`/`script` nodes.
- 2026-05-18: Subplan 04 of `01-security-access-control` added scoped assignment lifecycle overloads in `AssignmentService` and switched REST/HTMX lifecycle routes to pass the route `agentId` before cancel, pause, resume, or force interrupt mutation. Cross-agent route attempts now return 404-style lifecycle errors and do not mutate the target assignment; same-agent behavior and queue refresh rendering are preserved. Implementer checks passed with `mvn -Dtest=OrchestrationRuntimeTest,AgentOrchestrationControllerTest,OrchestrationControllerTest test`; bounded startup smoke reached successful Spring Boot startup before timeout shutdown. External validation is still pending.
- 2026-05-18: Subplan 04 validation passed on commit `f4b1978`: validation confirmed REST and HTMX lifecycle routes call scoped assignment service overloads, cross-agent attempts return non-2xx without mutation, and public controllers no longer call assignment-id-only lifecycle methods.
- 2026-05-18: `01-security-access-control` domain validation passed: focused changed-area tests, full `mvn test` with 467 tests, bounded Spring startup, live auth/CSRF/HTMX probes, and live `/workflows` XSS browser proof all passed. Evidence logs: `/tmp/magenta-security-domain-live-3317476.log`, `/tmp/magenta-security-domain-live-extra-3318140.log`, `/tmp/magenta-security-domain-playwright-3317476.log`.
- 2026-05-18: `02-workspace-tools-outputs` branch was created from the validated security integration tip. For subplan 01, interpret the plan's "yolo override" as an explicit opt-in operator override, not a default bypass of shell confinement.
- 2026-05-18: Subplan 01 of `02-workspace-tools-outputs` changed shell confinement so `*` in allowed shell commands is ignored unless `unsafeAllowWildcardShellCommands=true`, legacy profile seeding drops wildcard shell defaults when the override is off, and shell wrapper executables plus absolute/traversal argument patterns are rejected even under the unsafe wildcard override. Active orchestration contexts with `hostWorkspacePath` now resolve shell working directories against the run workspace, run output path, or current linked project scope instead of falling back to the broader agent workspace.
- 2026-05-18: Subplan 01 validation passed on commit `cddbb6c`: focused shell/config/runtime tests, config JSON parse, `git diff --check`, and bounded startup passed; validation confirmed wildcard shell commands are ineffective by default, the unsafe override remains constrained, active host workspace/output/project scope cwd resolution works, and `config/ai-config.example.json` no longer contains wildcard defaults or a real-looking API key.
- 2026-05-18: Subplan 02 of `02-workspace-tools-outputs` changed `AgentFileToolService` to resolve file roots per call from `OrchestrationTaskContextHolder`. Active contexts with `hostWorkspacePath` now use the assignment workspace by default, `outputs/...` for the active output directory, and `projects/<currentProjectId>/...` for the current project workspace; active agent-only contexts use that agent's workspace. The legacy data-root fallback remains only when no orchestration context is active. Validation passed on commit `0ad9c3d`: focused file/shell/registry/runtime tests, `git diff --check`, and bounded startup passed; validator confirmed active scope behavior and traversal/absolute/symlink escape protections.
- 2026-05-18: Subplan 03 of `02-workspace-tools-outputs` changed `AgentWebToolService` so production `web_fetch` uses a no-auto-redirect HTTP client and follows redirects manually. Each hop revalidates http/https scheme, host presence, DNS resolution, and private/local address rejection before sending the next request; redirect count is capped and malformed or private redirect targets fail before the redirected request is sent. `web_search` keeps the production redirect-capable client so search behavior is unchanged by the fetch hardening.
- 2026-05-18: Subplan 03 validation passed on commit `e914ad1`: focused web tool/registry tests, `git diff --check`, and bounded startup passed; validator confirmed direct private-host rejection, public-to-private redirect rejection, redirect loop cap, valid public redirect behavior, and malformed redirect target rejection.
- 2026-05-18: Subplan 04 of `02-workspace-tools-outputs` materializes leased project workspaces inside the active assignment temp workspace as `projects/<projectId>` symlinks. `PlanService.startRun` creates the link after host workspace/output allocation when the orchestration context carries a project id; shell/file project aliases in active assignment contexts now require the materialized link and verify it points at the leased project workspace. `OrchestrationRunnerService` removes the link before releasing the project lease, while terminal run temp cleanup also removes the assignment workspace tree. Implementer checks passed with focused workspace, file, shell, plan, and runtime tests; related lease/repository/registry tests; full `mvn test`; `git diff --check`; and bounded startup smoke.
- 2026-05-18: Subplan 04 validation passed on commit `65e51ad`: focused workspace/file/shell/plan/runtime tests, `git diff --check`, and bounded startup passed; validator confirmed membership and lease acquisition precede materialization, active assignment tool aliases require the materialized project link, cleanup removes the assignment-local link before lease release, and the canonical project workspace remains intact.
- 2026-05-18: Subplan 05 of `02-workspace-tools-outputs` changed `PlanService.startRun` so required filesystem workspace/output allocation failures save a terminal `FAILED` `PlanRun` with `errorText` and execution evidence instead of continuing as `RUNNING` with null paths. `ChatService` now returns that non-running task run without calling the model for blocking or streamed task execution. Implementer checks passed with `mvn -Dtest=PlanServiceTest test`, `mvn -Dtest=PlanServiceTest,OrchestrationRuntimeTest test`, `mvn -Dtest=WorkflowRunnerTest,TaskStreamSupportTest test`, `git diff --check`, and bounded startup on ephemeral port `33235`; parent validation is still pending.
- 2026-05-18: Subplan 05 validation passed on commit `82d6edb`: focused plan/runtime/workflow/stream tests, `git diff --check`, and bounded startup passed; validator confirmed allocation and project-link materialization failures persist terminal failed runs with operator-visible `errorText`/execution evidence and ChatService short-circuits non-running startup runs before model execution.
- 2026-05-18: Subplan 06 of `02-workspace-tools-outputs` hardened `OutputArtifactService` file_path materialization. Candidate source paths still resolve bare and relative names against the run output directory, but materialization now resolves the source with `toRealPath()` and requires the real target to stay under the real data root before copy or artifact registration. Missing files and broken symlinks produce explicit `IllegalArgumentException`s, escaped symlinks are rejected, and same-source/destination detection uses no-follow existence checks plus real-path comparison to avoid registering escaped output-directory symlinks. Implementer checks passed with `mvn -Dtest=OutputArtifactServiceAttributionTest test`, `git diff --check`, and bounded startup on ephemeral port `45683`.
- 2026-05-18: Subplan 06 validation passed on commit `a7cd293`: focused output artifact tests, `git diff --check`, and bounded startup passed; validator confirmed realpath data-root confinement before copy/registration, escaped symlink rejection, broken symlink and missing-file clear failures, and valid file copy behavior.
- 2026-05-18: Subplan 07 of `02-workspace-tools-outputs` updated `PlanService` output attribution fallback to recognize current `agents/{agentId}/workspace/outputs/{slug-run}` output directories while keeping legacy `agents/{agentId}/outputs/{slug-run}` parsing. Explicit `OrchestrationTaskContext` attribution now remains authoritative field-by-field, with missing agent ids filled from the output path when available; project-only contexts count as attribution context. Implementer checks passed with `mvn -Dtest=PlanServiceTest,OutputArtifactServiceAttributionTest,WorkspaceRepositoryAttributionTest test`, `git diff --check`, and bounded startup. Parent validation/review is still pending and the working tree is intentionally uncommitted.
- 2026-05-18: Subplan 07 validation passed on commit `3020dac`: focused plan/output/repository attribution tests, `git diff --check`, and bounded startup passed; validator confirmed current workspace-layout fallback attribution, explicit field precedence, partial context merge, filtered artifact views, system-agent exclusion guard, and legacy parser branch preservation. Residual: legacy layout and system output behavior are inspected in code but do not have dedicated regression tests.
- 2026-05-18: `02-workspace-tools-outputs` domain validation passed on commit `a426897`: focused domain test set passed with 169 tests, full `mvn test` passed with 499 tests, `git diff --check` passed, and bounded Spring startup reached port `37111`. Evidence logs: `/tmp/domain02-focused-tests.log`, `/tmp/domain02-full-mvn-test.log`, `/tmp/domain02-git-diff-check.log`, `/tmp/domain02-bounded-startup.log`. Playwright was intentionally skipped because this domain gate is backend/tool/filesystem only.
- 2026-05-18: Subplan 01 of `03-execution-history-streams` changed public plan/task/workflow run semantics to assignment submission. `/api/chat/{conversationId}/plan/execute` and `/api/chat/{conversationId}/plan/execute/stream` now reject direct public execution with a 400 directing clients to save and send to an agent. `/api/plans/{planId}/submit`, `/api/plans/{planId}/runs/stream`, `/api/tasks/{taskId}/runs/stream`, `/api/workflows/{workflowId}/runs`, and `/api/workflows/{workflowId}/runs/stream` now default public submit priority to `9` and create queued `TASK_RUN` or `WORKFLOW_RUN` assignments, preserving supported request agent/job/workspace/model/priority fields in assignment fields and input metadata. Chat `Execute now` was removed from the public client in favor of `Send to agent`; the stale `magenta-tools.js` run helpers also default priority to `9`.
- 2026-05-18: Subplan 01 validation initially failed one browser assertion because a clean `/workflows` empty state had no visible submit-to-agent control until a workflow was created or selected. The fix added deterministic empty-state workflow actions: a real HTMX `New Workflow` action and a disabled `Submit to Agent` placeholder, while preserving saved-workflow-only submission semantics.
- 2026-05-18: Subplan 01 Playwright revalidation passed browser assertions but exposed a warm-DB workflow history 500 from a legacy `workflow_runs` table missing graph-run columns such as `current_node_index`. Added a narrow additive `WorkflowRepository` migration for missing selected run columns and an in-memory legacy-run regression test so `/workflows` can render history fragments against warm databases without waiting for the later schema ownership domain.
- 2026-05-18: Subplan 01 validation passed on commit `c03a6f5`: focused controller/repository tests passed with 110 tests, `git diff --check` passed, bounded startup reached `Started Magenta2Application`, and Playwright browser-origin validation against `/chat`, `/plans`, and `/workflows` passed with no console/page errors and no unexpected Spring log `ERROR`, `Exception`, `SQLITE_ERROR`, `500`, or `current_node_index` entries. Evidence logs: `/tmp/magenta2-startup-validation.log`, `/tmp/magenta2-playwright-app.log`.
- 2026-05-18: Subplan 02 of `03-execution-history-streams` removed the saved-plan execution transcript wipe. `ChatService.resolveSavedPlanExecution` no longer calls a plan-service memory clear, the destructive `PlanService.clearConversationForExecution` helper was removed, and execution instructions no longer claim a fresh chat context. Added `ChatServiceTest.resolvingSavedPlanExecutionPreservesExistingTranscriptRows` to prove pre-existing persisted user/assistant messages remain after saved-plan execution resolution marks the plan executing. Implementer checks passed with `mvn -Dtest=ChatServiceTest,PlanServiceTest test`; stale-copy search found no remaining public `Execute now` or execution-clearing transcript copy.
- 2026-05-18: Subplan 02 validation passed on commit `a853c7c`: focused chat/plan/frontend tests passed with 31 tests, stale-copy search found only negative assertions, `git diff --check` passed, and bounded startup reached `Started Magenta2Application`. Validator confirmed explicit conversation delete still clears memory/metadata while saved-plan execution resolution preserves durable transcript rows. Residual: no separate run-scoped model memory store was introduced.
- 2026-05-18: Subplan 03 of `03-execution-history-streams` found `PlanController.streamRun` already preserved the subplan-01 submit-to-agent contract by creating a queued `TASK_RUN` assignment and emitting semantic `submitted`/`failed` SSE events. Added controller-level SSE capture coverage in `PublicRunSubmissionControllerTest` for successful assignment submission and no-agent submission failure, asserting the emitted stream text contains `event:submitted` or `event:failed` and never `event:TaskExecutionEvent`. Focused validation also keeps existing `TaskStreamSupport` semantic mapping coverage.
- 2026-05-18: Subplan 03 validation passed on commit `ad49b3e`: focused SSE tests passed with 15 tests, static scan found no class-name SSE wrapper in production `PlanController`, `git diff --check` passed, and bounded startup reached `Started Magenta2Application`.
- 2026-05-18: Operator instruction for future subagents: use `gpt-5.5` with medium reasoning for implementation subagents and `gpt-5.5` with high reasoning for review/validation subagents unless superseded by a later operator instruction.
- 2026-05-18: Subplan 04 of `03-execution-history-streams` changed public job run starts to submit high-priority `JOB_RUN` assignments instead of creating direct `job_run` rows. The HTMX `Start Run` control posts to assignment creation and renders assignment details, the job submit form now defaults to priority `9`, and REST `POST /api/jobs/{jobId}/runs` returns a queued `WorkAssignment`. `JobService.startRun` remains available for `OrchestrationRunnerService` while executing queued `JOB_RUN` assignments. Implementer checks passed with `mvn -Dtest=PublicRunSubmissionControllerTest,OrchestrationControllerTest,OperationalUiContractControllerTest test`, `git diff --check`, and bounded startup.
- 2026-05-18: Subplan 04 validation passed on commit `cb3b6e2`: validator confirmed no public `api/web` controller path calls `jobService.startRun`, REST and HTMX job run starts create queued `JOB_RUN` assignments with default priority `9`, existing run history still renders from stored job runs, and internal runner execution still owns `JobService.startRun`. Browser-origin `/jobs` proof used local Playwright CLI after MCP browser returned `ERR_BLOCKED_BY_CLIENT`; the live page created a job, submitted `Start Run`, and rendered `Assignment Created`, `Type: JOB_RUN`, and `Priority: 9` without 500s or console/page errors.
- 2026-05-18: Subplan 05 of `03-execution-history-streams` centralized schedule/reaction assignment-template parsing in `AssignmentTemplateParser`. `ScheduleService.save`, `EventReactionService.save`, schedule polling, reaction handling, and direct assignment creation now share the same assignment type parsing plus required-reference validation: schedules default to `JOB_RUN`, reactions default to `REPORT`, `JOB_RUN` requires a job id, `TASK_RUN` requires `input.taskId`, and `WORKFLOW_RUN` requires `input.workflowId`. HTMX schedule/reaction routes continue to render inline service validation errors, and REST routes continue to map save-time validation failures to `400`. Parent review corrected the schedule form display default back to `JOB_RUN`; focused tests, static scan, `git diff --check`, and bounded startup passed. Parent validation is still pending.
- 2026-05-18: Subplan 05 validation failed on commit `348f639` because the HTMX create-reaction form still selected `JOB_RUN`, conflicting with the service/parser default of `REPORT` and causing default reaction submits to require a job id. Fix pass changed the reaction form default to `REPORT` and added `OrchestrationControllerTest.reactionFormDefaultsAssignmentTypeToReport`.
- 2026-05-18: Subplan 05 revalidation passed on commit `c3db8a4`: validator confirmed centralized save-time/runtime parser reuse, required-reference rejection for `JOB_RUN`, `TASK_RUN`, and `WORKFLOW_RUN`, REST 400 validation errors, inline HTMX errors, schedule form default `JOB_RUN`, reaction form default `REPORT`, valid schedule/reaction saves, focused tests with 134 passing tests, static scan, `git diff --check`, bounded startup, and browser-origin checks against a live app on port `18080`.
- 2026-05-18: Domain `03-execution-history-streams` validation passed on commit `68a1ac99c207246d4ea76e00a0147a18df78f8e8`. Validator ran focused domain tests with 213 passing tests, full `mvn test` with 511 passing tests, `git diff --check`, bounded startup with isolated DB `/tmp/domain03-startup.sqlite`, and browser-origin sweep against live app on port `18080` with isolated DB `/tmp/domain03-browser.sqlite`. Evidence files: `/tmp/domain03-bounded-startup.log`, `/tmp/domain03-browser-evidence.json`, `/tmp/domain03-console-errors.json`, `/tmp/domain03-network.json`, `/tmp/domain03-live-app.log`. Browser proof covered `/chat`, `/plans`, `/workflows`, `/jobs`, and agent schedule/reaction surfaces; direct chat plan execution rejected with 400; public plan/task/workflow/job submissions created assignments with expected type/priority; plan/task/workflow SSE emitted semantic `submitted`; schedule/reaction save-time validation and defaults passed. Residual risk: no live model-backed saved-plan execution was run; transcript preservation remains covered by focused service tests.
- 2026-05-18: Subplan 01 of `04-workflow-authoring-runtime-js` split workflow draft persistence from executable validation. `WorkflowService.saveDefinition` now uses draft structural validation, while `saveDefinitionValidated`, `/workflows` validate/submit, public workflow run submission, and service run paths still use strict executable validation. The HTMX workflow route form now includes condition selection and persisted route rows can update `APPROVED`/`REJECTED` conditions without JS transport. Implementer and parent checks passed with `mvn -Dtest=WorkflowRunnerTest,OrchestrationControllerTest,PublicRunSubmissionControllerTest test`, static draft/save/validate route scan, `git diff --check`, and bounded startup reaching `Started Magenta2Application` on port `40027`; delegated validation/browser proof is still pending.
- 2026-05-18: Subplan 01 validation failed on commit `8e5e26a` because the live `/workflows` page still loaded `workflows.js`, which replaced `#workflow-editor-container` with the graph composer and API transport. Backend HTMX fragments passed direct browser-origin probes, but the visible page could not prove HTMX-owned route condition editing. Fix pass removed the `workflows.js` module attachment from `/workflows`, preserving the server-rendered HTMX editor as the live authoring surface; the static JS file remains for domain 04's later JS-island cleanup subplans.
- 2026-05-18: Subplan 01 validation passed on commit `bb606da`: validator confirmed draft persistence uses structural validation, validate/submit/run paths remain strict, `/workflows` no longer loads `workflows.js`, server-rendered HTMX authoring is the live page surface, incomplete approval/task drafts save without editor validation errors, strict validation blocks incomplete validate/submit/run paths, route condition editing works through HTMX forms, and a completed approval graph validates. Focused tests passed with 90 tests, `git diff --check` passed, bounded startup passed on port `36529`, and browser evidence was written to `/tmp/workflow-validation-bb606da-pass.json`.
- 2026-05-18: Subplan 02 of `04-workflow-authoring-runtime-js` tightened executable workflow validation in `WorkflowValidator.validate` while leaving `WorkflowValidator.validateDraft` unchanged. Empty drafts still persist through `WorkflowService.saveDefinition`, but executable validation now requires at least one node, exactly one start node with no incoming dependency routes, and reachability from that start path; public workflow submit/run routes and HTMX validate/submit already use `validateGraph`, while service run paths call `requireExecutable`. Implementer and parent checks passed with `mvn -Dtest=WorkflowRunnerTest,OrchestrationControllerTest,PublicRunSubmissionControllerTest test`, static call graph scan, `git diff --check`, and bounded startup reaching `Started Magenta2Application` on port `40005`; delegated validation/browser proof is still pending.
- 2026-05-18: Subplan 02 validation passed on commit `4f2eb4f1f31e438bb5de5e9d0225f41124dfad1b`: validator confirmed empty drafts still save but HTMX validate/submit, REST run, and SSE run reject with the executable-node error and do not create runs; disconnected two-root workflows reject before run creation; valid one-node `FINAL_OUTPUT` workflows validate; service/draft call graph remains split. Focused tests passed with 96 tests, full `mvn test` passed with 520 tests, `git diff --check` passed, bounded startup reached `Started Magenta2Application` on port `40461`, and live browser/API/SSE evidence was written under `/tmp/workflow-validation-4f2eb4f-*`.
- 2026-05-18: Subplan 03 of `04-workflow-authoring-runtime-js` narrowed `src/main/resources/static/js/orchestration/workflows.js` into a dormant graph-canvas/layout utility. The active `/workflows` page still does not load the file, and the file no longer auto-mounts on `data-orchestration-page='workflows'` or performs raw `fetch` CRUD/validation against `/api/workflows`; workflow create/update/delete/validate/submit remain owned by the server-rendered HTMX routes. Remaining JS is justified only for local graph canvas, route preview, side-panel editing, and pointer-drag behavior if a future caller explicitly instantiates `window.MagentaWorkflowGraphComposer`. Implementer checks passed with focused workflow page/static JS tests, static transport scan, `node --check`, `git diff --check`, and bounded startup; parent validation is still pending.
- 2026-05-18: Subplan 03 validation passed on commit `aa30176`: validator confirmed `/workflows` remains HTMX/server-owned and does not request `workflows.js`; `workflows.js` exposes only an explicit `window.MagentaWorkflowGraphComposer` graph canvas/layout/drag utility with no `fetch`, `/api/workflows`, `saveWorkflow`, `validateWorkflow`, `DOMContentLoaded`, or auto-mount markers; tests and changelog document the remaining JS justification. Focused tests passed with 76 tests, `node --check`, `git diff --check`, bounded startup reached `Started Magenta2Application` on port `46011`, and browser-origin proof created a draft/editor swap without console errors, page errors, 4xx/5xx responses, stale asset 404s, or server `ERROR` output.
- 2026-05-18: Subplan 04 of `04-workflow-authoring-runtime-js` changed workflow editor failure rendering so title, node, and route persistence errors return the current persisted HTMX target fragment with an explicit error banner instead of a tiny standalone status. Node/route fragment failures reload persisted workflow state before rendering, full-editor failures keep the persisted editor visible, and validation exceptions render inside `#workflow-validation-result`. No workflow CRUD/validation JavaScript transport was reintroduced. Implementer checks passed with `mvn -Dtest=OrchestrationControllerTest,WorkflowGraphComposerSecurityTest test`; static scans confirmed `/workflows` still does not load `workflows.js` and `workflows.js` still has no `fetch`, `/api/workflows`, `saveWorkflow`, `validateWorkflow`, `DOMContentLoaded`, or workflows auto-mount markers. Parent validation is still pending.
- 2026-05-18: Subplan 04 validation passed on commit `09fd41c`: validator confirmed title/node/route persistence failure paths render visible error banners while preserving persisted/current HTMX target state, validation exceptions render inside a `warnings` validation fragment, empty draft save remains allowed while validate/submit reject executable-empty workflows, `/workflows` remains HTMX-owned with no `workflows.js` request or Graph Composer text, and title/summary XSS probes stay escaped. Focused tests passed with 80 tests, `node --check`, `git diff --check`, static scans, live startup on port `18080`, browser-origin draft/editor and negative failure probes, and clean console/server logs.
- 2026-05-18: Domain `04-workflow-authoring-runtime-js` validation passed on commit `48e3f4f`: validator ran focused workflow/controller/runtime/public-run tests with 101 passing tests, full `mvn test` with 524 passing tests, `node --check`, static scans confirming no `/workflows` `workflows.js` load and no JS CRUD/validation transport markers, `git diff --check`, bounded startup with isolated SQLite, and browser-origin Playwright on port `18080`. Evidence files: `/tmp/domain04-focused-tests.log`, `/tmp/domain04-full-mvn-test.log`, `/tmp/domain04-node-check.log`, `/tmp/domain04-static-workflows-js-load-scan.log`, `/tmp/domain04-static-js-transport-scan.log`, `/tmp/domain04-git-diff-check.log`, `/tmp/domain04-bounded-startup.log`, `/tmp/domain04-browser-evidence.json`, `/tmp/domain04-network.json`, `/tmp/domain04-console.json`, `/tmp/domain04-live-app-validation.log`. Browser proof covered empty validate/submit/SSE-run rejection, incremental HTMX workflow CRUD, route condition editing, final approval workflow validation, visible node/route error banners, no `workflows.js` request, no Graph Composer text, and inert XSS payloads in workflow/title/node fields. MCP Playwright Basic-auth navigation was blocked, so the validator cleared the stale MCP lock and used local Playwright with `httpCredentials`; some CRUD probes used browser-origin HTMX-style `fetch` with `HX-Request` headers for deterministic server-owned path coverage.
- 2026-05-18: Subplan 01 of `05-schema-data-ownership` removed deprecated `workspace_roots` creation from clean `schema.sql` and changed `workspace_leases` to reference `workspaces(id)`. `WorkspaceRepository` now treats `workspace_roots` as a guarded warm-DB migration only: it copies legacy roots into `workspaces`, rebuilds `workspace_leases` only when the existing FK still targets `workspace_roots`, preserves active, release-requested, and released rows, then drops the legacy roots table. Implementer checks passed with `mvn -Dtest=WorkspaceLeaseServiceTest,WorkspaceRepositoryAttributionTest,WorkspacePathSegmentValidationTest,WorkspaceRepositorySchemaMigrationTest test`; validator proof is still pending.
- 2026-05-18: Subplan 01 validation passed on commit `e5606f7`: validator confirmed clean schema no longer creates `workspace_roots`, `workspace_leases.workspace_id` references `workspaces(id)`, and warm legacy Spring startup migrates `workspace_roots` while preserving active, release-requested, and released lease rows with holder, mode, expiry, release flags, release timestamps, and audit timestamps intact. Guard probes also confirmed current-FK lease rows remain intact when `workspace_roots` is absent or when stale roots still exist. Focused tests passed with 27 tests, full `mvn test` passed with 526 tests, `git diff --check` passed, and clean/warm isolated DB probes passed.
- 2026-05-18: Subplan 02 of `05-schema-data-ownership` made clean `schema.sql` canonical for the current plan/output drift targets: `plan_runs.temp_workspace_path`; `run_output_artifacts.agent_id`, `job_id`, `project_id`, `workspace_id`, and `run_type`; and the repository bootstrap attribution indexes on agent/job/project/workspace. Guarded repository add-column migrations remain for warm DB compatibility. Added `WorkspaceRepositorySchemaMigrationTest.schemaSqlCreatesCurrentPlanAndOutputArtifactShape`, which applies `schema.sql` to clean SQLite before repository construction, verifies the target columns/indexes, then constructs `PlanRepository` and `WorkspaceRepository` and asserts those column lists do not change. Implementer checks passed with `mvn -Dtest=WorkspaceRepositorySchemaMigrationTest,WorkspaceRepositoryAttributionTest,PlanRepositoryTest test`, a direct SQLite clean schema probe, `git diff --check`, and bounded Spring startup on an isolated SQLite DB. Inbox ownership and orphan `job_work_items` cleanup remain pending for later domain 05 subplans.
- 2026-05-18: Subplan 02 validation passed on commit `9f22d1a`: validator confirmed clean `schema.sql` contains `plan_runs.temp_workspace_path`, `run_output_artifacts.agent_id`, `job_id`, `project_id`, `workspace_id`, `run_type`, and run-output attribution indexes; post-startup isolated DB probes confirmed the same shape; `PlanRepository` and `WorkspaceRepository` warm add-column guards remain; and inbox ownership plus orphan `job_work_items` were not changed. Focused tests passed with 16 tests, full `mvn test` passed with 527 tests, `git diff --check` passed, and bounded startup reached `Started Magenta2Application` under the timeout wrapper.
- 2026-05-18: Subplan 03 of `05-schema-data-ownership` kept workflow/user inbox and runtime/agent inbox persistence separate because the active surfaces use different message models and lifecycle state. `schema.sql` now declares both `inbox_messages` and `agent_inbox_messages` with ownership comments and recipient indexes; `WorkflowRepository` and `OrchestrationRuntimeRepository` warm bootstraps create the same indexes. Added clean-schema tests that verify both table shapes and indexes, assert repository bootstrap does not change those target shapes, and prove workflow inbox messages and runtime direct-line agent inbox messages remain readable/writable on distinct surfaces. Implementer checks passed with `mvn -Dtest=WorkspaceRepositorySchemaMigrationTest test`, `mvn -Dtest=WorkspaceRepositorySchemaMigrationTest,OrchestrationRuntimeTest,WorkflowRunnerTest,OperationalUiContractControllerTest test`, a clean SQLite schema probe, post-startup SQLite inbox table/index probe, `git diff --check`, and bounded Spring startup on isolated SQLite. Orphan `job_work_items` cleanup remains pending for subplan 04.
- 2026-05-18: Subplan 03 validation passed on commit `62547aa`: validator confirmed clean `schema.sql` and post-startup DBs include `inbox_messages` and `agent_inbox_messages` with expected columns and recipient indexes; ownership is explicit in schema comments, subplan notes, and `.internal-dev/knowledge/inbox-table-ownership.md`; `WorkflowRepository` and `OrchestrationRuntimeRepository` bootstrap matches clean schema targets; workflow/user plus workflow-agent messages remain in `inbox_messages`; runtime direct-line agent messages remain in `agent_inbox_messages`; and cross-lookups return empty. Focused tests passed with 57 tests, full `mvn test` passed with 529 tests, `git diff --check` passed, and bounded startup/probes passed. `job_work_items` remained unchanged for subplan 04.
- 2026-05-18: Subplan 04 of `05-schema-data-ownership` found `job_work_items` only in clean `schema.sql`; no production repository, service, controller, or test reader/writer uses the table. Removed the clean-schema DDL and added clean schema coverage that `job_work_items` is absent while `orchestration_job_items`, `job_definitions`, and `job_runs` remain present. No warm-DB `drop table` migration was added because there is no current owner-approved data migration need for local legacy tables. Implementer checks passed with `mvn -Dtest=WorkspaceRepositorySchemaMigrationTest,OrchestrationRuntimeTest,OperationalUiContractControllerTest test`, clean and post-startup SQLite job-table probes, `git diff --check`, and bounded startup on isolated SQLite `/tmp/domain05-subplan04-startup.sqlite`.
- 2026-05-18: Subplan 04 validation passed on commit `e29e9b5fd09f4d5f48bebeea14cf11d0af314e94`: validator confirmed no production `src/main` hit for `job_work_items`, clean `schema.sql` keeps owned job tables `orchestration_job_items`, `job_definitions`, and `job_runs`, no destructive `DROP TABLE job_work_items` migration exists, clean and post-startup SQLite probes omit `job_work_items`, focused tests passed with 47 tests, full `mvn test` passed with 530 tests, `git diff --check` passed, and bounded startup reached `Started Magenta2Application` against isolated SQLite.
- 2026-05-18: Domain `05-schema-data-ownership` validation passed on commit `9560d6688daf42c2cb7780acd4eb7ff417e4bbdc`: validator ran focused schema/workspace/runtime/controller tests with 75 passing tests, full `mvn test` with 530 passing tests, `git diff --check`, clean `schema.sql` SQLite probe, clean bounded startup, warm legacy startup, current-FK warm startup with stale roots, inbox ownership static scan, and `job_work_items` static scan. Evidence directory: `/tmp/domain05-validation-9560d66/`. Clean and post-startup probes confirmed no `workspace_roots`, `workspace_leases` FK to `workspaces`, `plan_runs.temp_workspace_path`, run output attribution columns/indexes, both inbox table/index shapes, no `job_work_items`, and owned job tables present. Warm legacy startup migrated roots away and preserved active, release-requested, and released lease rows with holder/mode/expiry/release/audit fields intact. Current-FK warm DB with stale roots preserved lease rows and removed/migrated stale roots. The real local `chat-memory.db` was not mutated; warm behavior used isolated fixtures.
- 2026-05-18: Subplan 01 of `06-operational-ui-htmx-mobile` fixed bug-16 by adding an operational-shell mobile override in `src/main/resources/static/css/orchestration.css` for `.main-container.has-sidebar` at the SimplyPages phone breakpoint. The override forces a single `content` grid column, constrains `.content-wrapper`/`#content-area` to `minmax(0, 1fr)`/`width: 100%`, and leaves the framework off-canvas `.main-sidebar` behavior intact. `OrchestrationController` now loads `/css/orchestration.css?v=8`; implementer checks covered the focused controller/static CSS test, `git diff --check`, and bounded startup. Browser-origin mobile Playwright proof remains pending with the validation agent.
- 2026-05-18: Subplan 01 bug-16 validation failed on commit `342e051`: live Playwright at `390x780` on `/agents/e50f0ba0-7ece-4dc3-b17e-f65ca8624075` measured `.content-wrapper` and `#content-area` at `84px`, with `.main-container.has-sidebar` computed grid columns `84px 0px 250px` (`/tmp/domain06-subplan01-actual-mobile-measurements-342e051.json`). The fix keeps the one-column operational mobile override and also resets `.main-container.has-sidebar > .main-sidebar` to `grid-area: auto` at the phone breakpoint so the fixed/off-canvas sidebar no longer requests the missing `sidebar` named grid area and creates implicit tracks. `OrchestrationController` now loads `/css/orchestration.css?v=9`; focused tests/diff/startup are rerun by the implementer, and browser-origin mobile Playwright remains the validation-agent gate.
- 2026-05-18: Subplan 01 validation passed on commit `2064463`: validator reran `mvn -Dtest=OrchestrationControllerTest,FrontendControllerTest test` with 84 passing tests, `git diff --check`, bounded isolated startup (`/tmp/domain06-subplan01-2064463-startup.log`), and live app validation on port `18080` with isolated SQLite (`/tmp/domain06-subplan01-2064463-live.log`). Browser-origin Playwright at `390x780` on `/agents/699a84ee-109a-4acb-a3de-c9d4f0c97102` measured `#content-area` and `.content-wrapper` at `366px`, `.main-container.has-sidebar` grid columns `366px`, grid areas `"content"`, `.main-sidebar` `grid-area: auto`, document scroll width `390px`, no sidebar/content overlap, `/css/orchestration.css?v=9` loaded `200`, no stale asset 404s, zero console errors/warnings, no 4xx/5xx network responses, and desktop spot check preserved `250px 982px` `"sidebar content"` layout. Evidence: `/tmp/domain06-subplan01-2064463-browser-evidence.json`, `/tmp/domain06-subplan01-2064463-network.md`, `/tmp/domain06-subplan01-2064463-console.md`.
- 2026-05-18: Subplan 02 of `06-operational-ui-htmx-mobile` replaced stale agent lifecycle targets for dashboard Delete/Archive and confirmation actions. `/agents/{agentId}` dashboard now renders `#agent-lifecycle-panel-{agentId}` and lifecycle controls use HTMX `outerHTML` swaps into that visible panel; confirm actions use `?view=lifecycle` so disable/archive results return small visible lifecycle status fragments instead of full detail fragments or missing Docker targets. Stale `.agent-docker-status` CSS/test assumptions were renamed to lifecycle panel naming. Implementer checks passed with `mvn -Dtest=OrchestrationControllerTest test`, `git diff --check`, and bounded startup on isolated SQLite reaching `Started Magenta2Application` before timeout shutdown. Browser-origin HTMX proof remains pending with the validation agent.
- 2026-05-18: Subplan 02 validation passed on commit `23b1795`: validator reran `mvn -Dtest=OrchestrationControllerTest test` with 81 passing tests, `git diff --check`, bounded isolated startup, and live app Playwright MCP on port `18080` with isolated SQLite. Browser proof on `/agents/9d93d2a6-dde5-4838-9b24-4fdbcb9145e3` confirmed the initial visible `#agent-lifecycle-panel-{agentId}`, `Delete / Archive` targeting that panel with `outerHTML`, confirmation swapping into the same visible panel, `Disable Only` returning `200` with `HX-Request`/`HX-Target` and rendering `Agent disabled.`, stale target count `0` before and after swaps, Queue tab still usable, no changed `.js` files, no `/agents/_lifecycle` JavaScript transport, all lifecycle requests/assets `200`, console `0` messages, and no unexpected pre-shutdown Spring log errors. Evidence: `/tmp/domain06-subplan02-23b1795-controller-test.log`, `/tmp/domain06-subplan02-23b1795-startup.log`, `/tmp/domain06-subplan02-23b1795-live.log`.
- 2026-05-18: Subplan 03 of `06-operational-ui-htmx-mobile` changed bug-20 HTMX mutation failures to preserve their useful fragments while setting non-2xx statuses. Queue delete failures now set the existing assignment lifecycle error status (`409` for non-deletable running rows in focused coverage), shell exec unavailable/unexpected failures set `500` and validation failures set `400`, hard-delete confirmation/service validation failures set `400`, and settings validation failures set `400`. Unexpected shell/hard-delete/settings exceptions are logged in `OrchestrationController`. Implementer checks passed with `mvn -Dtest=OrchestrationControllerTest test` covering 85 tests, `git diff --check`, and bounded startup reaching `Started Magenta2Application` on isolated SQLite `/tmp/magenta-domain06-subplan03-startup.sqlite` before timeout shutdown.
- 2026-05-18: Subplan 03 validation failed on commit `df7f99d` because the browser received the correct settings-save `400` plus `.orch-status-error` fragment, but HTMX 1.9 emitted `htmx:beforeSwap status=400` with default `shouldSwap=false`, then `htmx:responseError`/`htmx:afterRequest`, leaving the operator error invisible (`visibleError=false`). The fix pass keeps the non-2xx controller semantics and adds a narrow `alpha-security.js` `htmx:beforeSwap` handler: same-origin non-401/403 HTMX error responses with an existing target and known operational markers (`.orch-error`, `.orch-status-error`, `.agent-lifecycle-panel`) opt into server-rendered fragment swapping. The existing 401/403 auth/CSRF banner path remains in `htmx:responseError`.
- 2026-05-18: Subplan 03 fix-pass implementer checks passed with `mvn -Dtest=OrchestrationControllerTest test` covering 86 tests, `node --check src/main/resources/static/js/alpha-security.js`, `git diff --check`, and bounded startup reaching `Started Magenta2Application` on isolated SQLite `/tmp/magenta-domain06-subplan03-swap-startup.sqlite`. Maven reported exit 143 after manual shutdown, after the startup proof had been observed.
- 2026-05-18: Subplan 03 validation passed on commit `1561406`: validator reran focused controller tests with 86 passing tests, `node --check src/main/resources/static/js/alpha-security.js`, `git diff --check`, bounded startup, and live Playwright/browser-origin checks on port `18080`. Settings HTMX failure returned `400`, included `.orch-status-error`, set `htmx:beforeSwap.shouldSwap=true`, kept `afterRequest.failed=true`, and visibly swapped into `#settings-form-container`; shell exec failure returned `400` with `.orch-error`; hard-delete validation failure returned `400` with `.agent-lifecycle-panel` and `.orch-error`; queue delete failure returned `409` with queue fragment/table, assignment id, and `.orch-error`. CSRF HTMX `403` retained `shouldSwap=false`, fired `htmx:responseError`, showed the CSRF banner, and did not swap the target. Direct browser dispatch confirmed both auth and CSRF banner text. The commit added no JavaScript CRUD/mutation transport, only the scoped HTMX swap-policy opt-in.
- 2026-05-18: Subplan 04 of `06-operational-ui-htmx-mobile` removed stale active runtime wording for ro-09. Active source/test/config/static surfaces now use filesystem/workspace runtime wording; unused container-runtime dependencies and the stale test runtime block were removed; workspace/runtime comments and test names were updated. Historical `.internal-dev` container-runtime evidence and explicit public-alpha remediation findings were left intact. Implementer checks passed with a clean active stale-label scan across `src/main`, `src/test`, `pom.xml`, `docs`, and `README.md`; `mvn -Dtest=OrchestrationControllerTest,AgentShellToolServiceTest,OutputArtifactServiceAttributionTest test`; `git diff --check`; and bounded Spring startup reaching `Started Magenta2Application` on isolated SQLite before timeout shutdown.
- 2026-05-18: Subplan 04 validation passed on commit `3a41d0b`: validator confirmed the required active-surface stale-label scan returned no matches across `src/main`, `src/test`, `pom.xml`, `docs`, and `README.md`; an extra scan including `config/` also returned no matches; `pom.xml` no longer has container-runtime/testcontainers hits; focused tests passed with 125 tests; `git diff --check` and `git show --check 3a41d0b` passed; and bounded startup reached `Started Magenta2Application` with evidence at `/tmp/domain06-subplan04-startup-pEMW4A/startup.log`. Playwright was skipped because no active UI/static stale label hits remained and no interaction behavior changed.
- 2026-05-18: Subplan 05 of `06-operational-ui-htmx-mobile` removed the static agent detail side-panel event log instead of inventing a new audit/event subsystem. Agent detail no longer renders `Event Log`, `Agent dashboard loaded`, `1 assignment waiting`, or static workspace-ready timeline items, and the detail layout now uses a full-width variant after the side panel removal. The agent dashboard now optionally uses `AgentWorkspaceStatusService` through `ObjectProvider<AgentWorkspaceStatusService>` to render health, path, writability, active runs/leases, linked projects, output artifact count/bytes, last activity, and status message; when the service is unavailable, the prior `Workspace: READY/PENDING` fallback remains. Implementer checks passed with `mvn -Dtest=OrchestrationControllerTest test`; validator proof is still pending.
- 2026-05-18: Subplan 05 validation passed on commit `670200c`: validator reran focused controller tests with 88 passing tests, placeholder scan found no production `Agent dashboard loaded`, `1 assignment waiting`, or `agent-event-log` matches, `git diff --check` and `git show --check 670200c` passed, and bounded startup reached `Started Magenta2Application`. Browser-origin Playwright on `/agents/1f9ce016-8cf9-44ee-8c53-4a9c7639622e` confirmed `/css/orchestration.css?v=10` and dashboard partial `200`, `.agent-event-log` count `0`, placeholder event text absent, `entity-detail-layout-full` full-width layout with no side panel, real `AgentWorkspaceStatusService` workspace fields visible, clean console/network, and clean Spring logs aside from expected manual shutdown.
- 2026-05-18: Domain `06-operational-ui-htmx-mobile` validation first failed on commit `9800a0a` because the stale-label gate still found active runtime wording: `AgentWorkspaceStatusService` comment `Replaces container status`, `AgentProfileService.enable` parameter `wakeContainer`, the matching controller-test stub parameter, and output attribution test fixture names/content `container-result.txt` / `container output`. All behavioral gate checks had passed. The serial fix renamed these to filesystem/workspace-neutral wording while leaving generic HTMX DOM container terminology intact.
- 2026-05-18: Domain `06-operational-ui-htmx-mobile` revalidation passed on commit `990fc12`: stale active runtime wording scans were clean, `node --check src/main/resources/static/js/alpha-security.js`, `git diff --check`, `git show --check HEAD`, focused tests with 127 tests, full `mvn test` with 539 tests, and bounded Spring startup passed. Live browser-origin validation on port `18080` with isolated SQLite `/tmp/domain06-reval-live/chat-memory.db` confirmed mobile `390x780` content width `366px`, lifecycle HTMX swaps into visible panels, operational HTMX error fragments on non-2xx statuses, CSRF 403 banner behavior, absence of placeholder event logs, and service-backed workspace health fields.
- 2026-05-18: Domain 08 subplan 01 resolved ro-06 by deleting the deprecated `src/main/java/io/mindspice/magenta2/ai/chat/workflow` package after active-source scans found no references outside the legacy package itself. Current workflow web/runtime/test callers remain on `io.mindspice.magenta2.ai.orchestration.workflow`. Implementer validation passed with active `rg` proof, `mvn -DskipTests compile`, `mvn -Dtest=WorkflowRepositoryTest,WorkflowRunnerTest,PublicRunSubmissionControllerTest,OrchestrationControllerTest test` (112 tests), and `git diff --check`.
- 2026-05-18: Domain 08 subplan 02 resolved ro-07/ro-08 implementation by deleting unused static modules `src/main/resources/static/js/magenta-tools.js`, `src/main/resources/static/js/orchestration/inbox.js`, and `src/main/resources/static/js/orchestration/outputs.js`. Active `/inbox` and `/outputs` remain HTMX/server-rendered fragments, and controller tests now assert those pages do not load the deleted modules. Implementer validation passed with active asset reference scans, stale direct-run static JS scan, `mvn -Dtest=OrchestrationControllerTest,FrontendControllerTest test` (93 tests), `git diff --check`, and bounded startup reaching `Started Magenta2Application` before timeout shutdown. No remaining changed JS files exist for `node --check`.

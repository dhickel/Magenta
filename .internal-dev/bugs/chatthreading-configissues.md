We have two issues that need to be addressed. Currently we do not support parrelell chats, we only support a single on and others wait for it. We need to allow for threaded requests accross our whole program, multiple model, or multiple agents calling the same mode can all occur. The only block we should have is on the chat itself. We already have a executor for offloading background task. We shoould build around this and plan out if we should have multipl executors, we should proably always have a main thread for updating and call the model via these a wait on ti so we have proper aborint? Idk this is going to be our production level code base so we need to handle multiple routes as we will have many users and each with multiple tastk at once.


This leads into another issue. We have issues where we have ollama configs, now I think deepseek configs and openai configs. But we can rotate between models. There seems to be hopefuly a small but possibly big issue in our code base where we mis use settings whichs leads me to beleive we have no singular code path being enforced for model initation and calling. We need to fix this. We have a router so I dont see how we are having issue. if we need to use better typing to identify things or possbly our own wrapper that can handle these. Im not sure the exact context of why we can just get the settings we need. We have an endpoint type set, so our router should be able to get the right cofig. Im hoping this is just a one off call outside the router but we need to fix this issue look for any in the same vain and ensure we enforce using a router for configuring and getting the model to call. We also need to enforce the use of our threaded executor erxecution of a call on the thread and model/endpoint side should all go through these systems. They need to be robust and they need to be our soul code path. The issue with the ollama config when calling deepseek could just be a small mishap from where we just pulled in a fix that was not uyet merged and back ported it so keep that in mind. But it coud be a code smell from multiple paths. Review this well, fix these issues, stengthen our contracts around execution and model routing and move us into a robust system.

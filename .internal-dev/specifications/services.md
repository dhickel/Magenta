---
schema_version: 1
document_type: services-specification
status: active
owner: services
created: 2026-05-25
---

# Services Specification

## Intended Contract

Services own use-case behavior and hide persistence, transport, filesystem, and model-provider details from callers. Add service behavior only for concrete user-facing workflows.

## Service Entries

| id | service_area | status | intended_contract | observed_anchors | ownership_boundary | drift_gaps | validation | related_decisions | related_knowledge |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SVC-20260525-06 | Workspace file explorer | active | Own confined create, rename, move, copy, preview/save, delete preflight/execute, labels (including typed metadata/description), and action log behavior. | `WorkAreaExplorerService`, metadata/log services | Controllers only map request/response and fragments. | Typed domain errors are deferred. | Service/controller tests. | `DECISION-20260524-03` | `workspace-file-explorer-details-list-rewrite.md` |
| SVC-20260525-07 | Shell command line parser | deferred | Strengthen `shell_exec` command line validation before wider exposure, explicitly handling or rejecting shell operators and per-command argument policy. | Shell tool service and command parsing | Process execution remains confined by workspace policy. | Current parser only handles whitespace, quotes, and backslash escapes. | Shell tool unit and security tests. | `DECISION-20260522-06` | `shell-tool-confinement-pattern.md` |
| SVC-20260525-08 | Web search/fetch | active | Web tools may use SearXNG for search and controlled fetch behavior for current-information tasks. | Web search/fetch services and config | Operational deployment details belong in docs/knowledge, not specs. | SearXNG host deployment is environment-specific. | Tool tests and live checks when config changes. | none | `web-fetch-redirect-validation-pattern.md` |
| SVC-20260525-09 | Task execution SSE | deferred | Consider native reactive or explicit executor-backed streaming if live task runs need higher concurrency. | Task execution SSE controller/service paths | Current blocking bridge is acceptable for current behavior. | HTTP threading scalability is deferred. | SSE tests and startup. | none | `plan-execution-stream-finalization.md` |
| SVC-20260525-10 | Planner recurrence | deferred | Planner tasks store recurrence and projections but do not automate reminders, user contact, wait-for-input, or assignments until separately designed. | Avatar planner organizer | Automation is out of v1 organizer scope. | Automation product policy unresolved. | Service/UI tests when accepted. | `DECISION-20260523-02` | `avatar-work-area-ui-refactor.md` |
| SVC-20260525-11 | Workspace leases | deferred | Current runtime uses exclusive writable project leases; read leases are future product/service decisions. Job-owned workspaces are legacy compatibility only and must not be extended as active product behavior. | Project workspace lease services | Read leases are not current runtime contract. | Future read-lease policy unresolved. | Lease service tests and runtime validation. | none | `project-workspace-lease-runtime-pattern.md` |
| SVC-20260525-12 | Browser chat pending message queue | active | Chat pending-message service owns validation, deterministic FIFO enqueue/list/claim/ack/release, stale claim recovery, and clear-conversation cleanup for normal `/chat` mid-turn messages, including concurrent same-conversation enqueue without duplicate order keys. | `ChatPendingMessageService`, `ChatPendingMessageRepository`, `ChatService.clearConversation()` | Controllers delegate queue use cases; queue persistence stays outside `ActiveTurnRegistry` and `ai_chat_memory`; repository/schema startup owns legacy duplicate order normalization before uniqueness is enforced. | Saved plan chat and agent side-panel chat are out of scope for this queue. | Repository/service/controller tests, schema warm-start duplicate normalization, concurrent enqueue uniqueness tests, browser drain validation. | none | `chat-context-management-and-tools.md`, `chat-planning-composer-architecture.md` |
| SVC-20260525-13 | Chat model defaults and aliases | active | File-configured `defaultModel` is the primary anonymous chat fallback before legacy default-agent model fallback. Model selector values are configured aliases; remote model names are resolved at the model-router boundary. | `AiConfig.defaultModel`, `RuntimeSettingsService`, `RequestResolver`, `ChatService`, `ChatModelRouter` | Preserve compatibility accepting either alias keys or remote names on request payloads. | Config loader, service/controller tests, startup smoke when config changes. | none | `spring-ai-model-options-routing.md`, `summary-title-model-selection.md` |
| SVC-20260526-01 | Workspace path layout and run output routing | active | Workspace services own application structural paths, Work Area disk path creation, run-local output staging, final output promotion targets, and compatibility handling for old path fields. `WorkspacePathLayout` is the source for static segments and aliases; `WorkspaceDirectoryService` remains responsible for data-root confinement and directory creation. | Workspace services, file/shell tools, task/plan execution, workflow runner, job service, output artifact service | Model-facing `outputs/` means current run-local `runs/<runId>/outputs/`; final destinations are written only by backend completion, validation, or promotion logic. Jobs bind to agent/project/Work Area and do not own directories. | Unit/service tests for layout helpers, Work Area id paths, run staging, output promotion, retention, and legacy compatibility. | `DECISION-20260526-01` | `workspace-file-architecture-rules.md`, `file-tool-workspace-scope-pattern.md` |
| SVC-20260526-02 | Runtime `AGENTS.md` resolver, starter generation, and prompt/context integration | active | Workspace/runtime services own first-create-only starter `AGENTS.md` generation for new agent workspaces, never overwrite existing files, resolve applicable files from bound root to active runtime target path, and provide ordered layers for prompt/context injection where closest wins only on conflict and ancestor guidance remains active. File and shell tool services publish the active runtime path from their confined target/working-directory resolution; prompt assembly consumes that service-owned path and does not traverse independently. | Workspace creation flows, runtime context services, file/shell tool services, prompt context assembly | Resolution must fail closed outside bound root and omit `AGENTS.md` context when no bound root exists. | Service tests for starter no-overwrite behavior, resolver matrix, tool-owned active path propagation, and prompt/context layering/de-emphasis behavior. | `DECISION-20260526-02` | `agents-md-specification-reference.md`, `workspace-file-architecture-rules.md` |
| SVC-20260526-03 | Agent Skills catalog, parsing, assignment, activation, and file-management services | active | Skill-domain services own root `skills/` discovery, `SKILL.md` parsing, metadata persistence, assignment metadata, runtime catalog disclosure, activation state, and root-confined API-facing file operations. MVP activation is agent-scoped only and uses dedicated activation tooling with dedupe. Script execution policies, project/user scope loading, layered assignment, and registry/package trust are deferred. | Implemented phase-02-06 `ai.skills` services and web consumers: `AgentSkillRepositoryService`, `AgentSkillParser`, `AgentSkillCatalogService`, `AgentSkillRepository`, `AgentSkillAssignmentRepository`, `AgentSkillAssignmentService`, `AgentSkillRuntimeCatalogService`, `AgentSkillPromptCatalogAssembler`, `AgentSkillActivationService`, `AgentSkillManagementService`, `SkillController`, `SkillFragments` | Controllers/chat remain consumers. Prompt assembly appends metadata-only catalog when assigned+enabled+loadable skills exist. Dedicated `activate_skill` returns parsed body-only instructions and resource listing without eager reads. File routes resolve skill slug/name through catalog metadata, enforce root confinement through repository service path guards, block traversal/symlink escapes, and map unsupported text operations distinctly. Browser optional-directory creation is limited to top-level `scripts`, `references`, and `assets`. `allowed-tools` remains experimental and must not be treated as enforced policy in MVP. | Parser/discovery/repository tests, assignment persistence/filtering tests, activation/dedupe tests, prompt-context tests, tool-registry/tool-access tests, controller/API/UI tests for file confinement/status mapping/fragments, full-suite `mvn test`, bounded startup, and reconciled Playwright browser evidence. | `DECISION-20260526-04`, `DECISION-20260526-05`, `DECISION-20260526-06` | `agent-skills-specification-reference.md`, `agent-skills-ui-htmx-pattern.md` |
| SVC-20260529-01 | Assistant dashboard widget instances | active | `AvatarService` owns widget add/settings validation against the dashboard widget registry: known widget type, supported width, row capacity through repository checks, single-instance policy, deterministic settings defaults, and settings validation errors. | `AvatarService`, `AvatarRepository`, `avatar.dashboard` registry | Repositories own SQL and migration mechanics; controllers render fragments only after service validation. | Repository/service/controller tests for migration, multi-instance, single-instance rejection, settings errors, and OOB settings save. | `DECISION-20260529-01` | `dashboard-fragment-navigation.md`, `simplypages-avatar-layout-and-editing.md` |
| SVC-20260529-02 | Avatar personal planner read models | active | `AvatarService` owns Today Planner, Tasks/Routines, and Calendar/Schedule read models that merge legacy todos/daily tasks/planner tasks/calendar items with day maps, time blocks, in-dashboard reminders, and recurrence occurrence status. Due dates, scheduled time blocks, reminders, and recurrence projections remain distinct service concepts. | `TodayPlannerView`, `TasksRoutinesView`, `CalendarScheduleView`, `AvatarService`, `AvatarRepository` | Reminder behavior is in-dashboard only; no external notification delivery or automatic assignment creation. | Repository/service/controller/tool tests for time blocks, reminders, occurrence skip/snooze/restart, calendar merged entries, and tool registration. | `DECISION-20260529-02` | `simplypages-avatar-layout-and-editing.md` |
| SVC-20260529-03 | Notes and project context widget read models | active | Notes widget read models distinguish Avatar-owned personal notes from file-backed notes. `ProjectArtifactService` owns fixed typed project artifact JSON defaults, validation, and confined project-root writes under `.magenta/project/`. Dashboard controllers render source/binding states and delegate reads/updates to services. | `DashboardNotesView`, `DashboardProjectContextView`, `ProjectArtifactService`, `AvatarDashboardController` | Personal notes remain in `avatar_notes`; file notes and project artifacts remain file-backed. Widget settings store source mode and last-opened references only. | Service/controller/tool tests for source binding, file-note read/edit, project artifact defaults/validation, and tool registration. | `DECISION-20260529-03` | `workspace-file-explorer-details-list-rewrite.md` |
| SVC-20260529-04 | Agent operational widget read models | active | Dashboard agent operational widgets query existing `AgentProfileService`, `AssignmentService`, `InboxService`, `OutputArtifactService`, `JobService`, `ProjectService`, `WorkAreaService`, and `WorkAreaExplorerService` APIs. Settings validation requires selected bindings for agent/project/job/Work Area modes, and Work Area file previews verify agent ownership before reading. | `AgentStatusQueueView`, `AgentOutputsView`, `AgentFilesNotesView`, `AvatarDashboardController` | Widget bindings are read-model filters only. They do not change normal `agent_*` tool context scoping and do not expose internal workspace/run roots. | Controller/tool tests for no-agent/missing-agent states, output scoping, scoped preview rejection, Work Area owner guard, and descriptor registration. | `DECISION-20260529-04` | `output-artifact-attribution-query-and-backfill-pattern.md`, `workarea-operational-ui-consistency.md` |
| SVC-20260529-05 | Avatar habits, reminder inbox, and dashboard context read models | active | `AvatarService` owns habit/tracker create/archive/log/history-correction behavior, reminder inbox complete/snooze/reschedule/skip/restart behavior, and dashboard context read-model data. Habit progress is non-punitive and supports build/quit semantics, period targets, target quantity/unit, display days/time range, optional streaks, and archived history. | `AvatarService`, `AvatarRepository`, `HabitsTrackersView`, `ReminderInboxView`, `AvatarDashboardController` | Reminder behavior is in-dashboard only; no external delivery and no automatic assignment creation. Dashboard Context is read-only and only displays visible registry descriptors. | Repository/service/controller tests plus startup and delegated browser proof. | `DECISION-20260529-05` | `simplypages-avatar-layout-and-editing.md` |
| SVC-20260531-01 | Chat and plan repository schema bootstrapping | active | Repository-owned warm-schema compatibility migrations use private whitelists for SQL identifiers and DDL fragments. User or caller values must not be concatenated into table, column, or type positions. | `ChatSessionMetadataRepository`, `AuditRepository`, `PlanRepository` | Services/controllers call named repository use cases only; repositories localize SQLite identifier constants and migration DDL. | Focused repository tests for malicious identifier payload rejection and normal schema/flag behavior. | none | `regression-gap-test-patterns.md` |
| SVC-20260531-02 | Orchestration assignment cancellation lease guard | active | Lease-owner writes are accepted only while an assignment row is still `RUNNING`, except for explicit `CANCEL_REQUESTED` to `CANCELLED` finalization. Late completion, failure, waiting, or checkpoint/progress writes must no-op once cancellation is requested. | `OrchestrationRuntimeRepository.saveAssignmentIfLeaseOwner`, `OrchestrationRunnerService` terminal helpers | Cancellation remains authoritative over in-flight worker results; force-interrupt and stale cancel recovery keep their dedicated repository paths. | Focused `OrchestrationRuntimeTest` coverage for late terminal/checkpoint writes, explicit cancel finalization, force-interrupt, and stale cancel recovery. | none | `orchestration-lease-heartbeat-and-task-sse.md` |
| SVC-20260531-03 | Assignment run display name validation | active | `AssignmentService` rejects new non-job `TASK_RUN` and `WORKFLOW_RUN` assignments without a nonblank user-visible run display name after controller context normalization and before persistence. | `AssignmentService`, `AssignmentTemplateParser` | Existing rows with null run display names remain readable. `JOB_RUN` submissions do not require route-supplied names because job context owns naming. | Focused controller/API and assignment service tests for missing-name rejection, valid non-job names, and job submission compatibility. | `DECISION-20260526-01` | `api-boundary-enums-and-empty-job-run-completion.md` |
| SVC-20260531-04 | Workflow PASS_THROUGH route materialization | active | `PASS_THROUGH` routes without ports are dependency routes that forward every source output key into downstream node inputs. Existing port-bearing `PASS_THROUGH` routes remain compatible as single-port mappings. | `WorkflowValidator`, `WorkflowRunner.resolveNodeInputs` | Canonical full-map pass-through requires no source/target ports. Port-bearing compatibility keeps old saved data runnable; partially ported pass-through routes are invalid. Node config remains the final input override after route materialization. | Focused `WorkflowRunnerTest` validation/runtime coverage for no-port full-map forwarding and legacy port-bearing compatibility. | none | `workflow-route-model.md` |

## Ownership Boundary

This file owns service behavior. API status codes belong in `api.md`; UI rendering belongs in `web.md`; dependency direction belongs in `service-graph.md`.

## Drift/Gaps

| id | status | observed_drift | routing | review_after |
| --- | --- | --- | --- | --- |
| DRIFT-20260525-04 | open | Workspace controller currently relies on message-based error mapping for some file explorer responses. | `DEFERRED-20260525-05` typed domain errors. | 2026-06-23 |

## Validation Expectations

Service changes require focused service tests. If Spring wiring or runtime dependencies change, run bounded startup. If the service is user-visible through web/API surfaces, add controller/browser validation as appropriate.

## Related Decisions

See `decisions.md`.

## Related Knowledge

Search knowledge filenames for `services`, `workspace`, `shell`, `web-fetch`, `task`, `lease`, `docker`, and `orchestration`.

---
schema_version: 1
document_type: web-specification
status: active
owner: web
created: 2026-05-25
---

# Web Specification

## Intended Contract

Magenta web surfaces are operational tools, not marketing pages. They should be dense, readable, HTMX-first where feasible, and validated with focused browser checks when UI behavior changes.

## Web Entries

| id | page_or_fragment | status | intended_contract | observed_anchors | interaction_rule | validation | related_decisions | related_knowledge |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| WEB-20260525-01 | Assistant dashboard home | active | `/` renders a user dashboard selector with `Assistant` selected, a compact chat rail, and configurable dashboard rows/widgets. Dashboards are agent-agnostic widget containers, not agent shells or Work Area owners. Dashboard selector/edit navigation swaps `#dashboard-home` from `/dashboards/{dashboardId}/_page` with URL push instead of reloading the full shell. | `/`, `/dashboards/{dashboardId}`, `/dashboards/{dashboardId}/_page` | HTMX-first fragments; narrow JS only for streaming, local resizing, or path-of-least-resistance behavior. | Controller tests plus focused Playwright screenshots and visual critique for UI changes. | `DECISION-20260524-01`, `DECISION-20260523-03` | `simplypages-avatar-layout-and-editing.md`, `avatar-shell-resizable-rail-geometry.md` |
| WEB-20260529-02 | Assistant dashboard widget instance surfaces | active | Widget summary roots use `#avatar-widget-{widgetInstanceId}` and `data-avatar-widget-type`. Detail and settings controls open into the shared `#avatar-edit-container`; settings save returns an OOB modal close and summary refresh. The widget catalog disables only single-instance types already on the dashboard and leaves multi-instance types addable. | `/dashboards/{dashboardId}/widgets/{widgetInstanceId}`, `AvatarDashboardComponents` | HTMX-first summary/detail/settings fragments; no full shell/nav/root returns for widget fragment swaps. | Controller tests plus delegated Playwright desktop/mobile proof. | `DECISION-20260529-01` | `dashboard-fragment-navigation.md`, `simplypages-avatar-layout-and-editing.md` |
| WEB-20260529-03 | Assistant dashboard planner widgets | active | Today Planner, Tasks/Routines, and Calendar/Schedule render as dense first-party dashboard widgets with summary bodies, detail/settings affordances, quick capture, non-punitive restart/skip/snooze controls, and a real calendar grid plus agenda structure. Calendar events, task due dates, time blocks, reminders, and recurrence projections must be visually/source distinguishable. | `AvatarDashboardComponents`, `/`, `/_dashboards/_today/*`, `/_dashboards/_planner-tasks/*`, `/_dashboards/_time-blocks`, `/_dashboards/_reminders` | HTMX-first fragments; no third-party calendar JS library; reminders are dashboard records only. | Repository/service/controller/tool tests plus delegated Playwright desktop/mobile planner proof. | `DECISION-20260529-02` | `simplypages-avatar-layout-and-editing.md` |
| WEB-20260529-04 | Assistant dashboard notes and project widgets | active | Notes widgets render clear source chips for personal, agent, project, Work Area, or mixed modes, handle missing bindings, provide personal quick capture/search/tag behavior, and open file-backed notes through confined viewer/editor fragments. Projects and Contacts/Materials widgets show selected-project household artifacts, contacts/materials, blockers, next actions, notes, outputs, and progress without duplicating full `/projects`. | `AvatarDashboardComponents`, `/dashboards/{dashboardId}/widgets/{widgetInstanceId}/_notes/*`, `/_file-note`, `/_project-artifacts/*` | HTMX-first fragments; file notes use Work Area/project file services; project widgets distinguish code projects from household projects. | Controller/service/tool tests plus delegated Playwright notes/project proof. | `DECISION-20260529-03` | `workspace-file-explorer-details-list-rewrite.md` |
| WEB-20260529-05 | Assistant dashboard agent operational widgets | active | Agent Status/Queue, Agent Outputs, and Agent Files/Notes widgets render explicit source chips and recoverable no-binding/missing-binding states. Outputs are scoped to selected agent/project/job/Work Area unless source mode is explicitly `dashboard`. Work Area file mini-views use service-confined owner guards and dashboard instance routes rather than legacy `/avatar/_work-areas` routes. | `AvatarDashboardComponents`, `/dashboards/{dashboardId}/widgets/{widgetInstanceId}/_outputs/{artifactId}`, `/_work-area-file` | HTMX-first fragments; dashboard selected-agent bindings do not alter normal-agent tool authorization. | Controller/tool tests plus delegated Playwright Phase 04 proof. | `DECISION-20260529-04` | `workspace-file-explorer-details-list-rewrite.md`, `output-artifact-attribution-query-and-backfill-pattern.md` |
| WEB-20260529-06 | Assistant dashboard habits, reminders, and context widgets | active | Habits/Trackers render build/quit trackers with period targets, quantity/unit, optional display days/time range, history correction controls, archive state, and non-punitive progress/trend/streak chips. Reminders/Alerts render an in-dashboard inbox with due/upcoming/snoozed summary, linked source display, complete/snooze/reschedule/skip controls, and explicit external-delivery-deferred copy. Dashboard Context is read-only and separates visible tool descriptors from approved chat/action behavior. | `AvatarDashboardComponents`, `/_dashboards/_habits*`, `/_dashboards/_reminders*`, `dashboard-context` widget | HTMX-first fragments; no external notifications; no automatic assignment creation; no context-panel action tools. | Repository/service/controller tests plus delegated Playwright Phase 05 proof. | `DECISION-20260529-05` | `simplypages-avatar-layout-and-editing.md` |
| WEB-20260531-02 | Home dashboard reusable template shells | active | Stable Home dashboard selector and dashboard panel shells may be rendered through SimplyPages `Template`/`SlotKey` with per-render `RenderContext` while preserving dynamic route/id attributes, `#dashboard-home`, `#avatar-widget-grid`, and full-page fallbacks. Highly dynamic widget bodies and row editor density remain normal component composition unless a stable template boundary is obvious. | `HomeDashboardTemplates`, `AvatarDashboardComponents.dashboardSelector`, `AvatarDashboardComponents.dashboardPanel` | HTMX-first selector/edit swaps retain fallback `href`, `hx-target="#dashboard-home"`, `hx-swap="outerHTML"`, and `hx-push-url`. | Controller/template tests plus delegated Playwright normal/edit dashboard proof. | `DECISION-20260531-01` | `simplypages-avatar-layout-and-editing.md`, `dashboard-fragment-navigation.md` |
| WEB-20260525-02 | Dashboard visual contract | active | Use dense panels, compact controls, thin blue-gray borders, low shadow, small radii, semantic chips, compact headings, and useful first-viewport density. Product-facing wording is Home dashboard/dashboard editor; `AvatarDashboard*` names are legacy implementation names until a deliberate rename is planned. | `/`, `/manage`, `/agents` | Avoid loose widget collages, hero sections, oversized personal-product cards, browser-default controls, and nested decorative cards. | Compare screenshots against `/manage` and `/agents`. | `DECISION-20260522-07`, `DECISION-20260531-01` | `simplypages-avatar-layout-and-editing.md` |
| WEB-20260525-03 | Assistant dashboard composition | active | The Assistant dashboard preserves the compact chat rail and in-place row/widget editor without an inner tab strip, Work Area widget, queue/history/output shell tabs, or Avatar banner/title. | Assistant dashboard baseline | Dashboard selection is addressable through `/dashboards/{dashboardId}`; edit mode stays dashboard-scoped. | Controller tests and browser rail/editor checks. | `DECISION-20260524-01` | `avatar-shell-resizable-rail-geometry.md` |
| WEB-20260525-04 | Work Area explorer | active | Use Magenta-local details/list fragments with separate inspector and stable table columns, full-row selection, and modal tag management from the inspector. Work Areas are exposed from agent detail and guarded by agent owner. | Agent detail Work Areas tab | OOB table/inspector/modal refreshes; direct service validation for paths/destinations and single-target tag params. | Service/controller tests and focused browser validation. | `DECISION-20260524-03` | `workspace-file-explorer-details-list-rewrite.md` |
| WEB-20260525-05 | Top navigation | active | Primary shell nav order is exactly `Home`, `Chat`, `Agents`, `Manage`; full-page shell nav uses full-page anchors unless HTMX swapping is explicitly intended. | `AppNavigation` and shell builders | Avoid accidental HTMX route swaps for full document navigation. | Controller tests. | none | `shell-navigation-htmx-vs-full-page.md` |
| WEB-20260525-06 | Agent history tab content | deferred | Replace static history placeholders with real agent run history when the query contract is accepted. | Agent detail history tab | Pagination/date filtering can be separate scope. | Controller/service/browser checks. | none | `agent-detail-workspace-health-pattern.md` |
| WEB-20260525-07 | Browser chat mid-turn queue | active | The `/chat` composer shows server-backed queued cards between the planning panel and input when normal messages are submitted during an active stream. The client must keep `?conversationId=` aligned with the active chat so plain reload restores queued cards, then retry draining until the existing active stream lock clears. Pending-card renders must be scoped to the active conversation and latest pending-list request so stale `CLAIMED` responses cannot repaint a drained queue. | `#chat-queued-messages-panel`, `chat-client.js`, `.planning-question-card` | Planning answers continue through plan answer routes; slash commands submitted mid-turn show wait feedback instead of queueing. Background queued drains may continue and ack after session switch, but must not steal the visible chat session or append messages into another active conversation. | Focused Playwright checks for slow-turn queueing, session-switch/away-and-return stale-card cleanup, reload persistence, FIFO drain, command/planning negatives, desktop/mobile screenshots. | none | `event-delegation-sse-dom-replacement.md`, `chat-planning-composer-architecture.md` |
| WEB-20260531-01 | Browser chat stream interrupt affordance | active | `/chat` may show stream interrupt controls when the stream `start` event includes `turnId` and `interruptToken`; those values are valid during plain model calls, tool model calls, and tool checkpoints. Client disconnect/error terminal paths must cancel abandoned model workers and allow the next stream for that conversation or plan execution to start without stale active-turn conflicts once the disconnect/error is observed. | `chat-client.js`, `/api/chat/stream`, `/api/chat/turns/{turnId}/interrupt` | Normal mid-turn messages still use the pending-message queue by default; explicit interrupt remains a separate stream-control affordance. Same-conversation overlap still rejects with the active-stream error while the prior stream is active, and elapsed time alone does not permit retry takeover. Chat SSE streams may include comment heartbeats for transport liveness; clients should ignore SSE comments and rely on named JSON events for state. | Focused browser validation should cover at least one plain stream interrupt call and one aborted/disconnected stream followed by a new turn. | none | `live-chat-mcp-workflow-testing.md` |
| WEB-20260525-08 | Browser chat model selectors | active | The `/chat` Agent Model and Planning Model selectors display configured model aliases as the visible option text and submit alias keys as values. Provider `remoteModelName` values remain routing details, not primary chat UI labels. | `FrontendController.chatToolbar()`, `ChatService.availableModelOptions()` | If a selected/default value is only known by remote model name, map it back to the configured alias before selecting the option. | Controller rendering tests and focused browser check when selector behavior changes. | none | `spring-ai-model-options-routing.md` |
| WEB-20260529-01 | Agents operational shell and selector | active | `/agents` and `/agents/{agentId}` render in an Agents-specific shell with primary top nav and no Manage side nav. Agent selector rows are compact HTMX rows with display name, one status chip, queue count, inbox count, selected state, and detail swaps into the right-hand panel. Selector/detail panes have a visible operational frame and divider. Lifecycle controls stay in agent detail manage areas, not selector rows. | `OrchestrationController.agents()`, `agentDetail()`, `agentList()` | Standard selection and filtering stay HTMX-first; JavaScript is limited to selected-row affordance and tab active state. | Controller tests plus focused Playwright desktop/mobile checks for `/agents` and `/agents/{agentId}`. | none | `agent-selector-shell-and-htmx.md` |
| WEB-20260526-01 | Workspace and project browser MVP | active | MVP file browsing/editing focuses on selected Work Areas and project directories. Work Area markdown/text editing uses a compact Avatar-style editor with explicit save, local undo/redo/revert controls, and unsaved markdown preview/split modes. Internal agent workspace roots, run staging, system outputs, and structural internals are not normal management surfaces; they may be read-only diagnostics in future scope. | Work Area explorer, project surfaces, output views | Browser interactions stay HTMX-first and service-owned; JavaScript is narrow and justified only when it remains the simpler path. | Focused Playwright screenshots and visual critique for changed file-browser/project surfaces. | `DECISION-20260526-01` | `workspace-file-explorer-details-list-rewrite.md` |
| WEB-20260526-02 | Agent Skills browser/editor and guided creation MVP | active | Provide operational `/skills` management surfaces for root-repository skills and per-agent assignment with HTMX-first fragments. UI must clearly separate implemented MVP behavior from deferred scope and must not imply UI script execution support. | `/skills` operational shell and fragments implemented for list/filter/detail, diagnostics, directory overview, file viewer/editor, add-file flow, optional top-level directory creation, guided scaffold creation, refresh, and agent assignment controls. | Standard CRUD/filter/row actions use HTMX; catalog-affecting mutations return OOB `#skills-list` refreshes; no custom skills JavaScript is required. MVP only manages root `skills/` repository plus agent assignment. | Controller/fragment tests cover shell/nav, list/filter, diagnostics, editor/save, file create, guided creation, assignment fragments, and OOB list refresh after catalog-affecting mutations. Phase 06 reconciled Playwright proof after first-run selector/path-script false negatives and recorded corrected evidence. | `DECISION-20260526-03`, `DECISION-20260526-06` | `agent-skills-specification-reference.md`, `agent-skills-ui-htmx-pattern.md` |

## Ownership Boundary

This file owns page/fragment UX contracts. SimplyPages component mechanics belong in `simplypages.md`; routes/payloads belong in `api.md`.

## Drift/Gaps

| id | status | observed_drift | routing | review_after |
| --- | --- | --- | --- | --- |
| DRIFT-20260525-06 | watching | Some history and observability panels are baseline or placeholder surfaces. | Deferred features before expanding UI claims. | 2026-06-24 |

## Validation Expectations

UI changes require focused Playwright validation under the repo policy, including screenshots and a visual quality critique. Browser checks should cover alignment, spacing, density, overflow, mobile stacking, and whether the page uses available space coherently.

## Related Decisions

`DECISION-20260522-07`, `DECISION-20260523-03`, `DECISION-20260524-01`, `DECISION-20260524-03`.

## Related Knowledge

Search knowledge filenames for `avatar`, `dashboard`, `shell`, `workspace-file`, `htmx`, and `simplypages`.
